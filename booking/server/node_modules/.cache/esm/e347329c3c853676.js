let express,readdirSync,cors,mongoose;_3c8‍.w("express",[["default",["express"],function(v){express=v}]]);_3c8‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_3c8‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_3c8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

const app = express();
// db connection
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
    useCreatInder: true,
  })
  .then(() => console.log("DB Connected"))
  .catch((err) => _3c8‍.g.console.log("DB Connection Error:", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json());
// route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

const port = process.env.PORT || 8000;
app.listen(port, () => console.log("server is running on  port  ${port}"));
